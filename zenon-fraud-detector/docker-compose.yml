services:
  db.zenom:
    image: mysql:9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
    ports:
      - '3306:3306'
    volumes:
      - mysql.zenom:/var/lib/mysql
volumes:
  mysql.zenom: